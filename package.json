{
  "name": "vscode",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rimraf ./packages/*/lib && rimraf ./packages/*/build && tsc -b",
    "build:watch": "rimraf ./packages/*/lib && rimraf ./packages/*/build && tsc -b -W",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch --maxWorkers=1"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^14.0.14",
    "@typescript-eslint/eslint-plugin": "^2.30.0",
    "@typescript-eslint/parser": "^2.30.0",
    "ts-jest": "^24.2.0",
    "ts-loader": "^7.0.5",
    "typescript": "^3.9.5"
  }
}
